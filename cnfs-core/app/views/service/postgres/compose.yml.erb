# postgres/compose.yml.erb
---
version: '<%= version %>'
services:
  postgres:
    labels:
      <%= labels %>
    image: mdillon/postgis:9.6
    ports:
      - <%= expose_ports(5432) %><% if env_files.size.positive? %>
    env_file:
      - <%= env_files %><% end %>
