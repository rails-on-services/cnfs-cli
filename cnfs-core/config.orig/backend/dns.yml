# config/backend/dns.yml
---
generic:
  endpoints:
    grafana: grafana
    api:
      scheme: https
      host: api
    sftp:
      host: sftp

development:
  <<: *generic
  root_domain: localhost
  sub_domain:

test:
  <<: *generic
  root_domain: localhost
  sub_domain:

production:
  <<: *generic
  root_domain: localhost
  sub_domain:
