# frozen_string_literal: true

module OneStack
  # class PluginCommand < Hendrix::PluginCommand; end

  class ProjectCommand # < Hendrix::ProjectCommand
    # binding.pry
    # register OneStack::PluginCommand, 'plugin', 'plugin', 'Create a new Hendrix plugin'
  end
end
