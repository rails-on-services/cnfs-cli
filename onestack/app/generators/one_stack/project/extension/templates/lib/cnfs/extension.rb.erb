# frozen_string_literal: true

module Cnfs
  module <%= name.camelize %>
    class Extension < Cnfs::Extension
      class << self
        def root() = Pathname.new(__dir__).join('../..')
      end
    end
  end
end
