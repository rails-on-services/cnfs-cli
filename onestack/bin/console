#!/usr/bin/env ruby
# frozen_string_literal: true

require 'bundler/setup'
Bundler.require

ARGV.unshift 'console'
ENV['ONE_STACK_ENV'] ||= 'development'
BOOT_MODULE = OneStack
Dir.chdir(Pathname.new(__dir__).join('../spec/dummy')) { require 'solid_support/boot_loader' }
