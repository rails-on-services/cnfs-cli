---
config:
  domain: cnfs.io
  host: host.${config.domain}
  targets:
    one: one.${config.host}
    two: two.${config.host}
  carry: ${name}.${config.targets.one}
segments_type: stack
default:
  resource_name: instance1
  segment_name: backend
