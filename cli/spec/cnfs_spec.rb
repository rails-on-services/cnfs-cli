# frozen_string_literal: true

RSpec.describe Cnfs do
  it 'has a version number' do
    expect(Cnfs::VERSION).not_to be nil
  end

  xit 'does something useful' do
    expect(false).to eq(true)
  end
end
