---
name: 1_users
paths:
  config: config
  data: data
  lib: lib
  src: src
  tmp: tmp
logging: warn
# order: stack, environment, target, namespace
config: {}
components:
  - { name: target, alias: t, default: lambda }
