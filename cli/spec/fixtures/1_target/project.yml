---
name: 1_target
config:
  paths:
    config: config
    data: data
    lib: lib
    src: src
    tmp: tmp
  logging: warn
  # order: stack, environment, target, namespace
  x_components:
    - { name: target, default: cap2, aliases: t, env: tar }
    - { name: namespace, default: cap2, aliases: n, env: ns }
