---
config:
  paths:
    config: config
    data: data
    lib: lib
    src: src
    tmp: tmp
  logging: warn
  x_components:
  - aliases: "-t"
    color: blue
    name: target
    default: crank
    env: tar
  - aliases: "-n"
    env: ns
    color: green
    name: namespace
    default: duf
type: Project
