---
iam:
  type: Rails::Service
  resource_name: res1
  config:
    test: test
  environment:
    - test
    - this
cognito:
  type: Rails::Service
  config:
    test: te
nginx:
  config:
    test: hello
