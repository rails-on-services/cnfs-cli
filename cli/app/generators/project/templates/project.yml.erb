---
name: <%= name %>
# config:
#   domain: cnfs.io
#   host: host.${config.domain}
#   targets:
#     one: one.${config.host}
#     two: two.${config.host}
# segment_type: target
# default:
#   provisioner_name: terraform
#   repository_name: cnfs-projects
#   resource_name: instance1
#   runtime_name: compose
#   segment_name: backend
