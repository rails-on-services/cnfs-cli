# redis/compose.yml.erb
---
version: '<%= version %>'
services:
  redis:
    labels:<%= labels %>
    image: <%= service.image[:name] || 'redis:5-alpine' %>
    ports:<%= expose_ports %><% if env_files.size.positive? %>
    env_file:
      - <%= env_files %><% end %>
