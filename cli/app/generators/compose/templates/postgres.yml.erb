# compose/postgres.yml.erb
---
version: '<%= version %>'
services:
  <%= service.name %>:
    labels:<%= labels %>
    image: <%= service.image[:name] || 'mdillon/postgis:latest' %>
    ports:<%= expose_ports %><% if env_files.size.positive? %>
    env_file:
      - <%= env_files %><% end %>
