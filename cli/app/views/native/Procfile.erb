# Generated by ./libexec/generate-config.rb

<% port = 5000 %>
<% map = { server: '_rails', worker: '_sidekiq', scheduler: '_scheduler', sqs_worker: '_sqs' } %>
<% services.select { |s| s.type.eql?('Service::Rails') }.each do |service| %>
<% service.profiles.each do |profile| %>
<%= "#{service.name}_#{profile}:\t#{map[profile.to_sym]}\t#{service.name} $PLATFORM_PATH #{port += 10}" %><% end %>
<%= "#{service.name}_t:\t_spring\t#{service.name} $PLATFORM_PATH" %>
<% end %>
