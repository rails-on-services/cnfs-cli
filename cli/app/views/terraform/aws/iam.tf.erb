# IAM
<%-
@module_name = :iam
def defaults
  {
    source: 'rails-on-services/eks-iam/aws'
  }
end
-%>

module "<%= title %>" {
<%= render_config(defaults).join("\n") %>

  # TODO: fix
  name   = local.iam_name
}

output "<%= title %>" {
  value = module.iam.*
}
