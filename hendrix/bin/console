#!/usr/bin/env ruby
# frozen_string_literal: true

require 'bundler/setup'

ARGV.unshift 'console'
ENV['HENDRIX_CLI_ENV'] ||= 'development'
Dir.chdir(Pathname.new(__dir__).join('../spec/dummy')) { require 'hendrix/boot_loader' }
