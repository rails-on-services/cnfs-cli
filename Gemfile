# frozen_string_literal: true

# Gems to install on a clean machine to use ./cnfs_gems
source ENV['GEM_SERVER'] || 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

gem 'thor'
gem 'guard'
