# IAM
<%-
@module_name = :iam
def defaults
  {
    source: 'rails-on-services/eks-iam/aws'
  }
end
-%>

module "<%= title %>" {
<%= resource_config(target, resource, defaults).render.join("\n") %>

  # TODO: fix
  name   = local.iam_name
}

output "<%= title %>" {
  value = module.iam.*
}
