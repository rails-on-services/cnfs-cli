# frozen_string_literal: true

module CnfsCli
  module Plugins
    class <%= name.camelize %>
      class << self
        def initialize_<%= name %>
          require 'cnfs_cli/<%= name %>'
          Cnfs.logger.info "[<%= name.camelize %>] Initializing from #{gem_root}"
        end

        # def customize
        #   src = gem_root.join('app/generators/<%= name %>')
        #   dest = Cnfs.paths.lib.join('generators/<%= name %>')
        #   FileUtils.rm_rf(dest)
        #   FileUtils.mkdir_p(dest)
        #   %w[service lib].each do |node|
        #     FileUtils.cp_r(src.join(node), dest)
        #   end
        # end

        def gem_root
          CnfsCli::<%= name.classify %>.gem_root
        end
      end
    end
  end
end
