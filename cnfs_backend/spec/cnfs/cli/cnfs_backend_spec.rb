RSpec.describe Cnfs::Cli::CnfsBackend do
  it "has a version number" do
    expect(Cnfs::Cli::CnfsBackend::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
