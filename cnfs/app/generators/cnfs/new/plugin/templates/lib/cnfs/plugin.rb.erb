# frozen_string_literal: true

module Cnfs
  module <%= name.camelize %>
    class Plugin < Cnfs::Plugin
      # initializer 'logger' do |app|
      #   Cnfs.logger.info('[<%= name.camelize %>] Initializing from', gem_root)
      # end

      class << self
        def gem_root() = Cnfs::<%= name.camelize %>.gem_root
      end
    end
  end
end
