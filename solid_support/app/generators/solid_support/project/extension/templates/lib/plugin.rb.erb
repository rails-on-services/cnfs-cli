# frozen_string_literal: true

module <%= gem_name_root.camelize %>
  module <%= name.camelize %>
    class Plugin < <%= gem_name_root.camelize %>::Plugin
      # initializer 'logger' do |app|
      #   Hendrix.logger.info('[<%= name.camelize %>] Initializing from', gem_root)
      # end

      class << self
        def gem_root() = <%= gem_name_root.camelize %>::<%= name.camelize %>.gem_root
      end
    end
  end
end
